package com.ningyuan.utils;

public class PagerUtil {

    public static void initPageInfo(Integer pageNum, Integer pageSize, Integer defPageSize) {
        CommonUtil.initPageInfo(pageNum, pageSize, defPageSize);
    }
}
